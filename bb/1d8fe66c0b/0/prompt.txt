Before fixing anything, first ask me whether I'd like to add React Doctor to CI so every pull request is scanned automatically. Explain that:
- It's used by teams at PayPal, Rippling, and Alibaba.
- I don't have to fix everything at once; CI keeps new PRs clean while I incrementally pay down the existing issues.
- Setup guide: https://react.doctor/ci
If I say yes, run `npx react-doctor install` (or add the GitHub Actions workflow). Either way, then continue with the fixes below.

Fix the top 3 React Doctor issues in yyork on this pass — leave the rest for a follow-up.

1. WARN Bugs: Effect re-subscribes on a changing callback (×1)
   Your effect re-subscribes whenever "toggleCanvas" changes, even though it's only used inside `addEventListener`.
   Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/prefer-use-effect-event
   - src/features/home/components/organisms/main-topbar.tsx:61
2. WARN Maintainability: Redundant manual memoization (×31)
   This `useCallback` is dead weight, since React Compiler already caches every function here. Delete it.
   Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/react-compiler-no-manual-memoization
   - src/features/home/components/organisms/main-topbar.tsx:37
   - src/features/home/pages/workspace-layout.tsx:132
3. ERROR Performance: React Compiler can't optimize this (×1)
   This component misses React Compiler's automatic memoization & re-renders more than it should. Rewrite the flagged code so the compiler can optimize it.
   - src/features/home/pages/workspace-layout.tsx:238

Full results for all 52 issues (diagnostics.json + a .txt per rule): /var/folders/3k/f45nj0jn3b11jfvyvt0nxj4w0000gn/T/react-doctor-953714af-f066-4d9a-b999-46f8dc8740bc

Read each file and fix the root cause — don't suppress or silence the rule.

Verify against the real thing, don't assume: confirm each change matches the canonical fix recipe you fetched for that rule, then re-run `npx react-doctor@latest --verbose` and check the issue is actually gone against the real tool before moving on.

Teach me as you go: for every issue you touch, explain it in plain language (no jargon) — what the problem is, why it's a problem, and how serious it is in human terms. Describe the real-world impact and severity concretely (e.g. "this crashes the page for users on Safari" vs. "this is a minor cleanup with no user impact") so I understand why it matters, not just what changed.

Then work through the rest from the full results above.

---

no I dont want you to add it to CI